var hero, bad_guy;

hero = 'strong';
bad_guy = 'weak';

if (hero === "strong") {
	if (bad_guy === "weak") {
		console.log('Ah-ha, an easy victory!');
	}
	else {
		console.log("Let us battle to the death!");
	}
}